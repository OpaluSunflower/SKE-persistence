package com.example.fotowoltaika.domain;

import org.springframework.data.jpa.repository.JpaRepository;

public interface PredictedReturnDateJPARepository extends JpaRepository<PredictedReturnDate,Long> {
}
